rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /responses/{docId} {
      allow write: if true;    // public write (Studienteilnahme)
      allow read: if false;    // no public read (Privatsphäre)
    }
  }
}
